# Claude Entry Point

Read `START_HERE.md`, then use `AI.md` for stable project truth and `STATE.md` for the current handoff.

Do not rely on chat history as the source of truth. Keep changes scoped, validate them, review the diff, and update `STATE.md` before finishing meaningful work.
